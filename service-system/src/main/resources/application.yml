spring:
  application:
    name: service-system
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

app:
  universal-login:
    enabled: ${APP_UNIVERSAL_LOGIN_ENABLED:false}
    password: ${APP_UNIVERSAL_LOGIN_PASSWORD:}
